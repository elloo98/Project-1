#include <iostream>
#include <string>
using namespace std;

/*int main() {
	int input;
	int store;
	printf("Enter a natural number: ");
	cin >> input;
	store = input;
	if (input < 1) {
		printf("%d is not a natural number, the program will now exit", input);
		return 0;
	}

	while (input != 0) {
		printf("%d ", input);
		input--;
	}

	while (input <= store) {
		printf("%d ", input);
		input++;
	}
	system("pause");
	return 0;
}*/