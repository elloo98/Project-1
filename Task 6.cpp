#include <iostream>
#include <string>
using namespace std;

/*int main() {
	int input = 0;
	int i = 3;
	int count;
	int c;

	printf("Welcome to the prime program.\nEnter a positive integer n to see the first n integers\n");
	cin >> input;

	if (input < 1) {
		printf("%d is not a possitive integer, the program will now quit\n", input);
		system("pause");
		return 0;
	}
	else
		printf("2 ");
	for (count = 2; count <= input; ){
		for (c = 2; c <= i - 1; c++){
			if (i % c == 0)
				break;
		}
		if (c == i){
			printf("%d ", i);
			count++;
		}
		i++;
	}
	printf("\nThe first %d prime number have been printed\n", input);
	system("pause");
	return 0;
}*/